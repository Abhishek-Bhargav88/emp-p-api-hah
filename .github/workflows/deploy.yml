name: Deploy to CloudHub

on:
  push:
    branches:
      - main

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '8'

      - name: Build and Deploy
        run: mvn clean deploy -DmuleDeploy
        env:
          anypoint.username: ${{ secrets.ANYPOINT_CLIENT_ID}}
          anypoint.password: ${{ secrets.ANYPOINT_CLIENT_SECRET}}
